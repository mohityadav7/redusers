<h1>{{user.first_name}} {{user.last_name}} [{{user.id}}]</h1>

<ul class="list-group">
  <li><strong>Email: </strong>{{user.email}}</li>
  <li><strong>Phone: </strong>{{user.phone}}</li>
</ul>

<a class="btn btn-info float-left mt-3" href="/">Back</a>

<form action="/user/delete/{{user.id}}?_method=DELETE" method="post" class="mt-3">
  <input type="submit" value="Delete" class="float-right btn btn-danger">
</form>
